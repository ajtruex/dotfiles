import { ExpoConfig } from '@expo/config';
export declare function setUpdatesVersionsAndroidAsync({ projectDir, exp, }: {
    projectDir: string;
    exp: ExpoConfig;
}): Promise<void>;
export declare function configureUpdatesAndroidAsync({ projectDir, exp, }: {
    projectDir: string;
    exp: ExpoConfig;
}): Promise<void>;
