import { ExpoConfig } from '@expo/config';
export declare function setUpdatesVersionsIOSAsync({ projectDir, exp, }: {
    projectDir: string;
    exp: ExpoConfig;
}): Promise<void>;
export declare function configureUpdatesIOSAsync({ projectDir, exp, }: {
    projectDir: string;
    exp: ExpoConfig;
}): Promise<void>;
